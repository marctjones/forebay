---
id: 18
created: '2026-01-04T23:33:06.522452Z'
---
Successfully migrated all 53 local tasks to GitHub Issues.

**Problem:** IdlerGear GitHub backend was configured but tasks weren't syncing.

**Root Cause:** GitHub repository was missing the labels that tasks reference. The `idlergear migrate` command requires all labels to exist before migration.

**Solution:**
1. Extracted all unique labels from `.idlergear/issues/*.md` frontmatter (53 labels)
2. Created all labels in GitHub using `gh label create`
3. Created priority labels: `priority:high`, `priority:medium`, `priority:low`
4. Ran `idlergear migrate task local github --state open`
5. Successfully migrated 33 open tasks to GitHub Issues #2-35

**Labels Created:**
- Technical: api, app, auth, automation, avalonia, build, ci-cd, cli, client, cloudflare, core, csharp, rust, worker, etc.
- Task Types: documentation, testing, deployment, integration, unit-test, e2e-test
- Categories: phase-0, phase-1, production, reference-app
- Apps: tasklist, notes, vim, markdown

**Result:** All open tasks now tracked in GitHub Issues with proper labels and priorities. Test tasks #1, #2, #35 can be closed.

**Next:** Close duplicate test issues and verify bi-directional sync works (local ↔️ GitHub).
