---
id: 25
created: '2026-01-05T01:48:39.265624Z'
tags:
- blockedissue
---
CLI API key migration partially complete but blocked by build issues.

Completed:
- Updated auth commands to use API keys instead of OAuth
- Updated ForebayClient to use SetApiKey() instead of SetSessionToken()
- Updated ForebayConfig to store ApiKey instead of SessionToken
- Removed LoginRequest, LoginResponse, LogoutResponse models
- Updated WhoamiResponse to remove ExpiresAt field
- Updated all queue commands to use API key authentication

Still needed:
1. Fix System.CommandLine 2.0.1 API compatibility issues in CLI
   - Argument constructors changed
   - SetHandler signature changed  
   - InvocationContext moved/changed
   - This is blocking ALL CLI builds

2. Update/remove obsolete tests:
   - ConfigManagerTests - update to test ApiKey instead of SessionToken
   - AuthenticationModelsTests - remove Login/Logout tests
   - ForebayAuthClientTests - remove LoginAsync/LogoutAsync tests

3. Test the updated client library (Core is building successfully)

Decision: Focus on Worker deployment first since CLI is blocked by framework issues.
